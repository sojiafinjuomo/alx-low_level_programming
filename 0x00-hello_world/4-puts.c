#include <stdio.h>

/**
 *  main- prints out programming is like building a multilingual puzzle
 *  puts-command used to display
 *
 *  Return: 0 always success
 */

int main(void)
{
	char MyProg[] = "\"Programming is like building a multilingual puzzle";

	puts(MyProg);

	return (0);
}

